{
  "name": "Martian City Hackathon ML",
  "install": "sudo apt-get update && sudo apt-get install -y python3.12-venv && python3 -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip && pip install -r requirements.txt && python -m src.martian.train",
  "terminals": [
    {
      "name": "api",
      "command": ". .venv/bin/activate && uvicorn src.martian.app:app --host 0.0.0.0 --port 8000"
    }
  ],
  "ports": [8000]
}
